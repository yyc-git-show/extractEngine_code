//火焰精灵类


(function () {
    var FireSprite = YYC.Class(YE.Sprite, {
        Init: function (data, bitmap) {
            this.base(bitmap);
        },
        Protected: {
        },
        Private: {
        },
        Public: {
        }
    });

    window.FireSprite = FireSprite;
}());